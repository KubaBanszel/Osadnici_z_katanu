import kostka
import pocetHracu
"""
# Define the dimensions of the board
rows = 8
cols = 8

# Create the board as a 2D list
board = []
for i in range(rows):
    row = []
    for j in range(cols):
        # If the row number plus the column number is even, the square is black
        if (i+j) % 2 == 0:
            row.append("██")
        # Otherwise, the square is white
        else:
            row.append("  ")
    board.append(row)

# Print the board
for row in board:
    print(''.join(row))
"""
